# Ticarium365

## Overview
Ticarium365 is a multi-tenant SaaS platform designed to offer comprehensive business management solutions including stock, barcode, sales, e-invoice, and marketplace management. Its primary purpose is to empower businesses with efficient inventory control, sales tracking, and streamlined customer/supplier relationship management. The platform features robust stock management with low-stock alerts, detailed sales analytics, subscription processing, and flexible role-based access control. It supports dynamic, tenant-specific branding and aims for continuous growth through advanced reporting, integrated barcode/labeling centers, and e-commerce catalog synchronization. The business vision includes expanding into a central B2B network for product discovery and RFQ, and providing an aggregator marketplace (Ticarium Pazar) for public access to curated products from suppliers.

## User Preferences
- I want iterative development.
- Ask before making major changes.
- Provide detailed explanations for complex solutions.
- Do not make changes to the `artifacts` directory unless specifically instructed.
- Do not make changes to the `lib/api-spec/openapi.yaml` file directly; it is the source for API client generation.

## System Architecture

The project utilizes a monorepo structure managed by `pnpm workspaces`.

### UI/UX Decisions
The frontend is built with React and Vite, employing Tailwind CSS and `shadcn/ui` for a modern, responsive user experience. It features dynamic branding, a persistent sidebar and header, a `TrialBanner` for trial accounts, and a multi-step `WelcomeTour` for user onboarding. The UI integrates POS terminals, data import wizards, loyalty program management, multi-currency handling, and comprehensive reporting dashboards. A public storefront (`/s/:slug`) allows anonymous visitors to browse products and place orders, adapting to tenant-specific themes and offering various payment integrations. UI elements like `EmptyState` components provide consistent user feedback, and a global command palette (⌘K) offers quick navigation. The login experience has been redesigned with a split-panel layout and enhanced password recovery workflow.

### Technical Implementations
The backend is developed with Express 5, utilizing PostgreSQL as the database, Drizzle ORM for database interactions, and Zod for schema validation. API clients and Zod schemas are generated from an OpenAPI specification using Orval. Authentication is session-based and scoped per company, ensuring tenant isolation. Barcode scanning is managed by `@zxing/browser`, and QR codes are generated with `qrcode.react`. The monorepo comprises `prosan` (frontend), `api-server` (backend), `lib/db` (database schema), and libraries for API specifications, clients, and Zod schemas. Features are controlled by a subscription-based feature flag system, enforced by `requireFeature(code)` middleware.

**Core Features:**
- **Multi-Tenancy:** Subdomain-based routing with `company_id` for database isolation.
- **User Roles:** Granular access control (`super_admin`, `admin`, `staff`, `viewer`).
- **Subscription & Payment:** Manages `trial`, `active`, `suspended` plans with `402 Payment Required` enforcement and audit logging for changes.
- **Stock Management:** Prevents negative stock, automates returns, and enforces unique product identifiers.
- **Sales Rules:** Role-based sales creation and return-only policies.
- **Error Handling:** Standardized JSON error responses and a global error handler.
- **Audit Logging:** Tracks critical system actions, including subscription changes.
- **Security:** Secure session cookies, rate limiting (including brute-force protection per IP + username), tenant isolation, idempotency for payments, and API-level business rule enforcement. Sensitive credentials are encrypted at rest using AES-256-GCM.
- **Omnichannel Sales Channels:** Product-channel listings with flexible pricing and stock engines, including an adapter framework for marketplace API integrations.
- **B2B Network:** Supports network profiles, reviews, a B2B directory, cross-tenant marketplace (`/b2b/vitrin`), and Request for Quote (RFQ) functionality for buyer-seller interactions. This includes quote response, comparison, and awarding mechanisms.
- **e-Fatura (Provider-Agnostic):** Multi-tenant e-Invoice/e-Archive module with a pluggable `EInvoiceProvider` interface supporting various providers. Features an outbox for outgoing and inbox for incoming invoices, with robust idempotency and state guards. Stub HTTP providers are implemented for testing.
- **Marketplace (Provider-Agnostic):** Multi-tenant marketplace integration framework with a `MarketplaceProvider` interface, supporting platforms like Trendyol, Hepsiburada, and N11. Includes product-channel mappings, pricing/stock rules, and a job queue for synchronization with enhanced error categorization and retry logic.
- **Profit Center & OCR:** Tenant-scoped financial overview with an OCR endpoint for automated expense form filling.
- **Accountant Panel:** Collaboration module with official report generation.
- **Data Import:** Supports CSV import for various entities.
- **POS Terminal:** Rapid sales interface for barcode-scanner focused operations.
- **Production & BOM:** Manages `production_recipes` and `production_orders` with atomic transactions for stock.
- **Loyalty & Points System:** Configurable loyalty programs integrated with sales.
- **Multi-Currency:** Manages `currency_rates` with historical data.
- **True Profit Engine:** Effective-cost model considering purchase price, holding costs, and capital cost, generating per-product profit snapshots and AI-driven recommendations.
- **Shipping Management:** Region and weight-based shipping engine with configurable zones, rules, and product overrides.
- **Pricing Engine:** Channel-based pricing rule engine with various modes and rounding options, supporting batch application and preview.
- **Aggregator (Ticarium Pazar):** Cross-tenant central marketplace listing chosen products from suppliers.
- **Ad Budget Tracking:** Multi-channel advertising expense tracking with ROI, CPA, CPC, and profit calculations.
- **Karlılık & Kanal Kıyas (Profit & Channel Comparison):** Analyzes sales performance by channel.
- **Public Marketing Site:** Provides anonymous access to marketing pages and contact forms.
- **Notification Hub:** A centralized service for in-app notifications, ensuring deduplicated and atomic delivery of system-critical events and budget alerts.
- **System Health Page:** Super admin-gated page providing internal health checks for DB, object storage, SMTP, and runtime metrics, including status, latency, and auto-refresh.
- **Setup & Usage Score:** A checklist-based scoring system for tenant onboarding and feature adoption, with deep links for completion and super-admin overview.
- **Company Profile:** Tabbed page for managing company identity, operations, fixed costs, performance metrics, and a built-in payroll calculator based on Turkish regulations.
- **Architecture Documentation:** `ARCHITECTURE.md` provides a high-level overview of integration lines, adapter patterns, worker patterns, and the finance ledger.

## External Dependencies

- **PostgreSQL**: Primary database for data storage.
- **Drizzle ORM**: Used for database interactions.
- **Zod**: Utilized for schema validation.
- **Orval**: Generates API clients and schemas from OpenAPI specifications.
- **express-session**: Manages user sessions.
- **bcryptjs**: For secure password hashing.
- **@zxing/browser**: Facilitates camera-based barcode scanning.
- **qrcode.react**: Generates QR codes within the UI.
- **Tailwind CSS**: Provides utility-first styling.
- **shadcn/ui**: Offers a collection of reusable UI components.
- **OpenAI Vision**: Integrated for OCR functionality, particularly for receipt processing.
- **multer**: Handles file uploads, used in data import features.
- **Sentry**: Optional integration for error tracking.
- **SMS Provider Adapter**: Pluggable interface for various SMS providers (e.g., NetGSM, İletimerkezi, Vatansms).
- **Expo Push**: Used for sending mobile push notifications.
- **Trendyol Sapigw**: API for marketplace integration, covering product, price, and order synchronization.
- **Hepsiburada MPOP API**: API for marketplace integration, covering product, price, and order synchronization.
- **N11 API**: API for marketplace integration, covering product, price, and order synchronization.
- **Paraşüt API**: Integrated for e-Invoice functionalities via OAuth2.
- **TCMB EVDS**: Provides exchange rate data.

## Sprint H (v3) — Test Suite Auth + Subscription Repair + Prod Hardening (TAMAM)
- `node --test integration.test.mjs` (484 test) tam yeşil — exit=0, 0 fail (concurrency=8, ~115s).
- **Auth seed kapsamı tam**: `seedDefaultUsers` artık 8 kullanıcı: `admin/admin123`, `talha/talha123`, `nihat/nihat123`, `nihat_admin/nihat123`, `cenan/cenan123`, `superadmin/superadmin123` (rol=`super_admin`, enum-uyumlu), `personel/staff123`, `goruntule/staff123`. Dev/test'te parola hash'leri otomatik tazelenir (`bcrypt.compare` mismatch → rehash).
- **Production bootstrap hardening**: `seedDefaultUsers` `NODE_ENV=production`'da otomatik çalışmaz; tek seferlik bootstrap için `SEED_DEFAULT_USERS=1` env override gerekir → deterministik default-credential expose riski kapatıldı.
- Test fixture standardize: `cenan→admin`, `admin123/nihat123/superadmin123` unified.
- Sprint 11 fixture v2 paket geçişi: testler `free/starter/pro/enterprise` → `pkg_inventory/pkg_trade/pkg_growth/pkg_enterprise_v2` (seed v2 legacy slug'ları otomatik deactivate ediyor — production migration korundu).
- **Kritik isolation fix (Sprint 11 STRICT after())**: PROSAN'ı `pkg_growth/yearly/active`'e geri yükler + assert-driven doğrulama (plans 200, growth bulunur, subscribe 201, /current → active+pkg_growth). Aksi halde Sprint 12/22/23/73 (documents/hr.staff/campaigns/marketplace.*) FEATURE_LOCKED 403 cascade'i.
- **Tenant string normalize**: line ~1166 "co2 tedarikçisi co1'den görünmez" `tenant="nihat"` → `"nihatturizm"` + supplier-create 201 explicit assert (silent early-return kaldırıldı, false-positive kapanışı).
- **Sprint 62 before() düzeltme**: einvoice plan bootstrap loop'u `["prosan", "nihat"]` → `["prosan", "nihatturizm"]` + nihat-side credentials `nihat_admin/nihat123` (önceden mismatch vardı).
- Architect 4 tur (FAIL → FAIL → PASS → PASS) — milestone kabul.
- **Deferred (sıradaki sprint'e)**: per-suite isolated-fixture rewrite, talha cross-suite plan-coupling refactor, clean-DB bootstrap re-validation. Bunlar 484-pass için bloklayıcı değil.

## Sprint H — Buyer Portal × PROSAN Birleşmesi (TAMAM)
- Buyer-portal sayfaları PROSAN içine taşındı: artifacts/prosan/src/pages/satinalma/{Discovery,NewRfq,Rfqs,SellerInbox,index}.tsx — tüm internal navigation /satinalma/* prefix'ine güncellendi (Discovery "Teklif İste" + NewRfq onSuccess redirect dahil).
- Tek API host, tek auth, tek subdomain — backend hiç değişmedi.
- App.tsx: 6 yeni ProtectedRoute (/satinalma, /satinalma/kesfet, /satinalma/rfqs, /satinalma/rfqs/new, /satinalma/rfqs/:id, /satinalma/inbox).
- nav-config.ts: NavGroup ve NavItem tiplerine accountTypes?: Array<"buyer"|"seller"|"both"> alanları eklendi. Yeni "satinalma" grubu (5 item, item-level accountType filtreli).
- layout.tsx + command-palette.tsx: visibleGroups → group-level + item-level accountType filtresi (her iki nav yüzeyinde tutarlı).
- E2E PASS: admin (both) login → tüm 5 satinalma item görünür; Discovery → "Teklif İste" → /satinalma/rfqs/new?sellerId=X; NewRfq submit → /satinalma/rfqs/:id (404 yok).
- Eski buyer-portal artifact bozulmadı (geçiş sürecinde yedek olarak duruyor).
- **C1 — Buyer-portal decommission (2026-04-20)**: Geçiş tamamlandığı için yedek artifact `artifacts/buyer-portal` tamamen kaldırıldı. Workflow + artifact registration + dizin silindi (`pnpm-workspace.yaml` `artifacts/*` glob, otomatik). Backend `routes/buyer-portal.js` (buyer/seller routes) PROSAN tarafından kullanıldığı için DOKUNULMADI. Re-test: **488/488 PASS, exit=0, ~67s**. Aktif artifacts: api-server, prosan, smsystems-mobile, mockup-sandbox.
- **C2 — smsystems-mobile audit (2026-04-20)**: Expo tabanlı, gerçek aktif mobil ürün (app/_layout.tsx, app/(tabs), app/login.tsx, @workspace/api-client-react bağımlılığı). Workflow `artifacts/smsystems-mobile: expo` aktif. Decommission KAPSAM DIŞI — canlı ürün olarak korunuyor. Sprint 70 mobil push entegrasyonunun konsumeri.

## Sprint B (Test Hardening) — Per-suite isolated fixtures + plan-coupling kırma + clean-bootstrap re-validation (TAMAM, 2026-04-20)

**B1 + B2 (per-suite isolated plan fixtures + talha cross-suite plan-coupling refactor)**:
- `tests/integration.test.mjs` üst seviyede yeni helper: `ensureTenantPlan(tenant, slug, cycle)` — idempotent, sadece mismatch varsa superadmin set-plan çağırır. `_saJarCache` ile süperadmin login bir kere yapılır.
- Cross-suite plan-coupling kıran defansif `before()` injection — Sprint 11 STRICT after()'a load-bearing bağımlılık kalktı:
  - Sprint 12 (Dosya/Evrak) — mevcut before() içine ensure (PROSAN→pkg_growth/yearly).
  - Sprint 21 (Akıllı Bildirim) — yeni before() eklendi.
  - Sprint 22 (Personel Yönetimi) — yeni before().
  - Sprint 23 (Kampanya & İndirim) — yeni before().
  - Sprint 73.6 (Reklam Bütçesi) — mevcut before()'a inject.
  - Sprint 73.7 (Ticarium Pazar) — mevcut before()'a inject.
- Sprint 11 after() restore-mantığı korundu (belt-and-suspenders) ama artık tek savunma hattı değil.

**B3 (clean-DB bootstrap re-validation)**:
- API server workflow restart edildi (cold start ~5s + seedDefaultUsers idempotent rehash). Restart sonrası **488/488 PASS, exit=0, ~64s, concurrency=8** — bootstrap akışı stable.

**B4 (bootstrap docs)** — Geliştirici için 1-page özet:
1. **Seed**: API server cold-start'ında `seedDefaultUsers()` 8 kullanıcı yaratır/refresh eder (admin, talha, nihat, nihat_admin, cenan, superadmin, personel, goruntule). Production'da `SEED_DEFAULT_USERS=1` env override gerekir (deterministik default-credential expose riski kapatıldı).
2. **Plan seeding**: `pkg_inventory/pkg_trade/pkg_business/pkg_growth/pkg_enterprise_v2` v2 paketleri seed; legacy `free/starter/pro/enterprise` deactivate. PROSAN baseline = pkg_growth/yearly; NİHAT bootstrap'ta plansız (testler kendi plan zeminini ensure'lar).
3. **Test fixture (tek-seferlik dev/test)**: Tek runner host (localhost:8080), tek `integration.test.mjs` (488 test, 63 suite). Her plan-sensitive suite `ensureTenantPlan(...)` ile kendi zeminini garantiler (cross-suite coupling yok).
4. **Sprint H (CAS)**: Concurrent set-plan'ler için per-company advisory lock + FOR UPDATE row lock + opsiyonel `expectedSubscriptionId` precondition. Test helper `__test_cancel_active` (NODE_ENV !== "production" guard) no-row baseline için.
5. **Komutlar**: `pnpm install` (root) → `pnpm --filter @workspace/api-server dev` → `cd artifacts/api-server && node --test --test-concurrency=8 tests/integration.test.mjs`.

Final: **488/488 PASS, restart-stable, plan-coupling kırılmış**.

## Master Backlog (mimari odaklı sıra)
| # | Sprint | Açıklama | Durum |
|---|---|---|---|
| 0 | Sprint 51-55, 62, 65, 70 + Frontend UI | E-Fatura UBL-TR + Bütçe & Tahmin + Marketplace hardening + canonical ledger + PROSAN UI | TAMAM |
| 1 | **Sprint A — Architect Regression Tests** | Forecast aliases (avg/slope/label ↔ legacy categoryName/trendSlope/forecast invariant + finiteness + totalForecast≈Σ tolerans) + months clamp [2,12] + invalid period 400 + e-fatura outbox `lastResponse.xml` (UBL `<Invoice` root, mock fallback numeric PayableAmount, non-TRY USD DocumentCurrencyCode propagation, multi-line + AllowanceTotalAmount discount math) — `tests/integration.test.mjs` describe "Sprint A — Architect regression: forecast aliases + outbox XML" **6/6 PASS**. **Sprint A re-validation (2026-04-19)**: Sprint 10 izolasyon testi `pkg_trade` 403 sorunu — 4-round architect iterasyonu sonunda **PASS** (Verdict: Sprint A DONE, with conscious CAS trade-off): (a) **Feature-based fail-CLOSED guard** — `GET /subscriptions/features` 200 değilse mutation skip; aksi halde `isAllUnlocked` veya `features.includes("finance.expenses")` doğru NO-OP (slug whitelist yerine semantic check, pkg_business/trial wildcard/future plans dahil). (b) **State-preserving teardown** — `before()` GET `/subscriptions/current`'tan nihatPrePlan capture; pre-state okunamazsa mutation YAPMA (risky pkg_trade fallback kaldırıldı). `after()` lost-update guard ile yalnızca mevcut plan hala UPGRADE_TARGET=`pkg_growth/monthly` ise restore. (c) **Multi-dimension deterministic delta invariant** — paymentMethod="bank" ile unique marker expense insert; nihat: totalExpenses=+marker, netProfit=-marker, revenue=0, totalCashBalance=0; cenan: hepsi 0 (4 dim × 2 tenant = 8 kausal cross-tenant leak invariant); finally cleanup DELETE. (d) Backend CAS/version precondition Sprint H ileri sertleştirme olarak deferred — sequential test runner'da TOCTOU low-prob. **484/484 PASS, exit=0, ~66s, concurrency=8** | TAMAM |
| 1.1 | **Sprint H (CAS/Version Precondition) — 3-round architect PASS (2026-04-20)** | Sprint A 4. round'dan deferred backend hardening: `/admin/billing/set-plan` lost-update koruması. **Round 1**: optional `expectedSubscriptionId` body field + TX içinde `SELECT ... FOR UPDATE` ile aktif/grace satır lock + CAS check; mismatch → 409 `subscription_version_mismatch` + `currentSubscriptionId`. Sprint 10 teardown CAS-aware (201 veya 409 OK). 2 yeni test (T1 roundtrip stale 409→refresh 201, T2 backward-compat). **Round 2**: per-company `pg_advisory_xact_lock(signedKey)` (key = `(0x53455450 << 32) | companyId & 0x7fff...`) — no-row case'de bile tx'leri serileştirir, null-precondition gap kapanır. 23505 unique-violation → 409 mapping (refresh select ile fresh `currentSubscriptionId`). 5-paralel race testi: aynı `expectedSubscriptionId` ile 5 concurrent set-plan → tam 1×201 + 4×409 deterministik. **Round 3**: 23505 mapping constraint adıyla daraltıldı (`company_subscriptions_active_per_company_uq` only) — diğer unique ihlalleri yanıltıcı sınıflanmaz. TEST-ONLY endpoint `POST /admin/billing/__test_cancel_active` (NODE_ENV !== "production" guard, requireSuperAdmin) ile no-row baseline. Null-precondition race testi: clear → 2 paralel `expectedSubscriptionId: null` → tam 1×201 + 1×409 + final invariant `conflict.currentSubscriptionId === winner.subscription.id`. **Architect Round 3 verdict: PASS**. Toplam delivery: backend (CAS + advisory lock + FOR UPDATE + narrowed 23505 mapping + test endpoint) + 4 yeni test (T1 CAS roundtrip, T2 backward-compat, T3 5-paralel race, T4 null-precondition race). **488/488 PASS, exit=0, ~64s, concurrency=8**. Architect optional follow-up (deployment hardening, ertelendi): explicit ENABLE_TEST_ENDPOINTS opt-in flag + NODE_ENV startup health-check + CI test that __test endpoint returns 404 in prod. | TAMAM |
| 1.5 | **Sprint I — Satınalma Hesabı (purchasing) refinement** | `accountType="purchasing"` üçüncü tip; `nav-config.isVisibleForAccount` whitelist; sade 5 item menü (Satınalma Merkezi · Menü Tercihleri · Marka & Logo · Firma Profili · Kullanıcılar); `/satinalma-merkezi` sayfası (filtre/arama/sort, xlsx+CSV+PDF export, favori toggle, iletişim/karşılaştırma modal, "Tekliflerim" link); `/b2b/catalog/marketplace` SADECE `isPublished=true` VE (`sourceProductId IS NULL` OR `products.stock > 0`) — onaylı + stoklu vitrin | TAMAM |
| 1.30 | **Dalga 23 — Kontör aşım enforcement & ek kontör (top-up) satışı + Architect R1→R3 PASS (2026-04-21)** | Aylık kontör paylarını aşım anında 402 ile engelleyen + ek kontör satışıyla artıran tam akış. **DB**: `usage_counters.purchased_credits` integer notNull default 0 + `credit_purchases` YENİ (companyId, paymentId, metric, packCode, quantity, unitPrice, totalAmount, period, status pending\|applied, appliedAt). **services/usage.ts**: `QuotaExceededError` (code='QUOTA_EXCEEDED'), `assertWithinUsageLimit(companyId, metric, by)` — planLimit=-1 sınırsız OK, planLimit=0+purchased=0 → 402, count+by > planLimit+purchased → 402; `incrementUsage` ON CONFLICT branch'i `effective = ${limit} + usage_counters.purchased_credits` üzerinden race-safe yeniden hesap; `addPurchasedCredits(companyId, metric, qty)` UPSERT — purchased += qty + overage stale değer için CASE ile yeniden hesaplanır (effective büyürse overage düşer). **services/billing/credit-packs.ts** YENİ: 10 paket katalog (einvoice 100/500/2000, ocr 50/200/1000, api 10k/100k, sms 500/2000) sabit fiyat TRY. **routes/billing.ts**: `GET /api/billing/credit-packs` (auth), `POST /api/billing/topup` admin — packCode validate → payments(billingCycle='topup', planId=0) + credit_purchases(status='pending') + provider checkout session; webhook handler `payment.succeeded` öncesi yeni branch: `payment.billingCycle==='topup' \|\| planId===0` → subscription'a dokunmaz, credit_purchases'i 'applied' işaretler, **TX commit sonrası** webhook caller `addPurchasedCredits` ile usage_counters'a yansıtır + invalidateFeaturesCache. Aynı branch __simulate-success'a da bağlı. **Hook'lar**: (a) `routes/profit.ts /receipt-ocr` ve `routes/finance-documents.ts /:id/ocr` → assertWithinUsageLimit('ocr',1) + 402 JSON `{error:{code:'QUOTA_EXCEEDED',metric,limit,current}}`, success sonrası incrementUsageSafe('ocr',1); (b) `services/sms/factory.ts sendSms()` → companyId>0 ise gating + quota_exceeded status'lı sms_messages kaydı (provider'a hiç gitmez), success sonrası incrementUsageSafe('sms',1); (c) `middlewares/api-key-auth.ts requireApiKey` → public-API tüm endpoint'leri için tek noktadan gating + incrementUsageSafe('api_calls',1). **E2E test**: talha login → GET credit-packs (10 paket) → POST topup ocr_50 (49 TL, conv UUID) → __simulate-success → `{ok:true,paymentId:3,status:succeeded,topup:true,applied:1}` → DB doğrulama: `usage_counters` company_id=1 metric=ocr period=2026-04 purchased_credits=50, `credit_purchases` status='applied' appliedAt set ✓. Frontend top-up satın alma sayfası Dalga 23B'ye bırakıldı (backend tamamen hazır, /api/billing/credit-packs + /topup ile entegre olacak). **Architect Round 1 → 2 fix**: (1) **Top-up apply atomicity**: ilk implementasyonda credit_purchases TX içinde 'applied' işaretleniyordu ama addPurchasedCredits TX-dışı çağrılıyordu → fail-open (apply başarısız olursa ödeme 'succeeded' kalır, kredi kaybolur). Ayrıca paralel webhook race testinde double-apply gözlendi (purchased_credits 100 yerine 200 oldu). **Fix**: credit_purchases TX içinde **'pending' kalır**; idempotent webhook self-heal mantığı eklendi (status='succeeded' + pending purchase varsa retry). `applyTopupsAfterCommit` artık atomik **claim** yapar: `UPDATE credit_purchases SET status='applying' WHERE id=X AND status='pending' RETURNING id` — sadece bir thread satırı kapar; addPurchasedCredits sonrası 'applied'; başarısız olursa 'pending'e geri al. **Race test**: aynı topup için 5 paralel webhook → final purchased_credits=200 (tek apply ✓), credit_purchases status='applied' ✓. (2) **api-key-auth fail-closed**: assertWithinUsageLimit quota dışı hatada (DB/servis arızası) önceden istek devam ediyordu (fail-open enforcement bypass). Artık 503 USAGE_CHECK_UNAVAILABLE döner. **Architect Round 2 → 3 fix (period rollover)**: top-up satın alma ay sınırını aşan webhook retry'larında `currentPeriodUTC()` kullandığı için yanlış aya yazılabiliyordu. **Fix**: `addPurchasedCredits(companyId, metric, qty, period?)` opsiyonel period parametresi (regex `^\d{4}-\d{2}$` validate, geçersizse currentPeriodUTC fallback geriye dönük uyumluluk için); `_applyTopups` payload'larına credit_purchases.period eklendi (hem yeni-succeeded hem self-heal path'leri); applyTopupsAfterCommit period'u taşır. **Verify**: yeni topup → credit_purchases.period='2026-04' → usage_counters period='2026-04' purchased_credits=300 ✓. **Architect Round 3 verdict: PASS** ("Resolved high-severity period rollover bug ... Implemented optional period parameter with validation and fallback"). | TAMAM |
| 1.29 | **Dalga 22 — Iyzico Recurring Billing (mock-first adapter, 2026-04-21)** | Sıfırdan ödeme altyapısı: mock-first adapter pattern + Iyzico iskelet (gerçek API key gelince tek env ile prod). **DB schema** (`lib/db/src/schema/payments.ts` YENİ): `payments` tablosu (companyId, planId, billingCycle, amount numeric(12,2), currency, provider 'mock'|'iyzico', status 'pending'|'succeeded'|'failed'|'refunded', conversationId UUID UNIQUE, externalId, rawRequest/rawResponse jsonb, errorCode/Message, paidAt, subscriptionId soft-ref, createdAt/updatedAt) + indexler (conversation uniq, company+createdAt, status). **Servis** (`api-server/src/services/billing/iyzico.ts` YENİ): `BillingProvider` interface (createCheckoutSession + verifyWebhookSignature + parseWebhookEvent), `MockBillingProvider` (header `x-mock-signature: mock-ok`, callback URL'e simulate=success param ekler), `IyzicoBillingProvider` placeholder (HMAC-SHA256 over rawBody), factory `getBillingProvider()` env'e göre seçim. **Routes** (`api-server/src/routes/billing.ts` YENİ): (a) `POST /api/billing/checkout` admin auth — plan validation (active + isPublic + requiredAccountType match) + pending payment insert + provider session açar, paymentPageUrl döner; (b) `POST /api/billing/webhook` no-auth (HMAC verify) — `db.transaction` + `pg_advisory_xact_lock(hashtext(conversationId))` ile per-conversation race koruması, idempotent (succeeded/refunded ikinci çağrıda no-op), payment.failed → status update, payment.succeeded → subscription pro-rate (`max(now, currentExpiresAt) + periodMs` → kullanıcı upgrade'de zaman kaybetmez) + 23505 unique violation retry-as-update path; (c) `GET /api/billing/payments` auth — son 50 ödeme; (d) `POST /api/billing/__simulate-success` DEV-only super_admin/admin (cross-tenant guard: payment.companyId === session.companyId). **Wire-up**: `routes/index.ts` `router.use("/billing", billingRouter)`; `app.ts` `express.json verify` callback `req.rawBody` byte sırasını korur (Iyzico HMAC için kritik). **Frontend**: `prosan/src/pages/odeme-sonuc.tsx` YENİ — callback sayfası (loading/success/error states, mock'ta otomatik __simulate-success çağrısı, gerçek Iyzico'da `/api/billing/payments` polling); `App.tsx` `<Route path="/odeme/sonuc">`; `pricing.tsx` `subscribe()` artık `/api/billing/checkout` çağırıp `paymentPageUrl`'e yönlendiriyor (eski instant /subscribe akışı yerine ödeme akışı). **E2E test (mock)**: login talha → checkout pkg_pro yearly (18990 TL, conversationId UUID) → 3 paralel __simulate-success → `1 succeeded + 2 idempotent` (advisory lock ✓), subscription_id 1664 active yearly, expiresAt = +365d; sonra pkg_starter monthly checkout → 3 paralel simulate → 1 succeeded + 2 idempotent + plan 13→12 + cycle yearly→monthly + expiresAt 2027-04-21 + 30g = 2027-05-21 (pro-rate ✓ kalan 365g zarar görmedi). pkg_procurement (is_public=false) → 403 PLAN_HIDDEN ✓. **Architect Round 1 → 6 issue (race/raw-body/pro-rate/accountType/cross-tenant/money) → fix → Round 2 PASS** ("None remaining"). Production'a geçiş: env IYZICO_API_KEY+IYZICO_SECRET_KEY set + IyzicoBillingProvider'ın createCheckoutSession metodunda gerçek `iyzipay` SDK çağrısı (Dalga 22B). | TAMAM |
| 1.28 | **Dalga 21 — /paketler Redesign + AI Öneri Widget (2026-04-21)** | Eski hardcoded 5 paket veri kaldırıldı; sayfa artık `useQuery /api/subscriptions/plans` ile gerçek 4 satılan paketi (`pkg_starter/pkg_pro/pkg_business_v3/pkg_enterprise_v3`) çekiyor. **Aylık/Yıllık toggle** ("2 ay hediye" badge'i ile yıllık ödemede). **Plan kartları** `<PlanCard>`: 6 limit göstergesi (kullanıcı/şube/ürün/e-belge/pazaryeri/depolama emoji ile), `parseFeatures()` ile ilk 6 highlight + "+N özellik daha", `Sparkles` ringli "Sana önerilen" badge veya "En çok tercih edilen" badge (popular = pkg_pro). **AI Öneri Widget** (`<AdvisorWizard>`): 4 sorulu form (kullanıcı sayısı number, aylık e-belge number, marketplace evet/hayır, çoklu firma evet/hayır) → kural-tabanlı `recommendPlanSlug()` (multiCompany=yes → enterprise; users≥15 veya einvoice≥1000 → enterprise; users≥6/einvoice≥400/marketplace=yes → pro veya business; users≤2/einvoice≤80 → starter; default pro). Defensive fallback: önerilen slug listede yoksa pkg_pro veya plans[0]. Submit sonrası kart + tabloda primary ring ile vurgulanır + "Senin için en uygun paket: X" mesajı. `Math.max(1,...)` ile negatif input koruması. **Karşılaştırma tablosu** (`<ComparisonSection>`): `buildFeatureMatrix()` backend feature kodlarını dinamik gruplar (Stok&Ürün / Satış&Cari / Finans&Resmi / Büyüme&Pazaryeri / Kurumsal / Diğer) + 10 satırlık limit row (kullanıcı/şube/ürün/e-belge/OCR/API/marketplace/müşteri/depolama/aşan e-belge ücreti). FEATURE_LABELS map ile snake_case kodlar TR'ye çevrilir, sortOrder + alfabetik. Önerilen plan kolonu primary/5 background ile vurgulanır. **Architect Round 1 PASS**: response shape uyumlu (features text→parse), edge-cases korunmuş (negatif clamp, empty list guard), XSS yok (numerik state + React escape), null-safety React-default. Önerilen non-blocking fallback `pkg_pro→plans[0]` round 2'de eklendi. Mevcut `/pricing` (in-app, subscribe akışı) ayrı kaldı; `/paketler` public landing-style (PublicNav + PublicFooter + İletişim CTA). | TAMAM |
| 1.27 | **Dalga 20 — Trial Otomasyonu: 7g/3g/1g uyarı + 0g status transition (2026-04-21)** | Trial abonelikleri için saatlik tarama + otomatik bildirim + status geçişi. **Yeni servis** (`api-server/src/services/trialWatcher.ts`): `runTrialWatcherTick()` saatlik node setInterval ile çalışır + boot'tan 30s sonra ilk kick. Tüm tick `pg_try_advisory_lock(7136423)` altında — multi-instance/concurrent trigger güvenli, lock-busy görürse skip + log `trial_watcher_tick_skipped_lock_busy`. Akış: (1) `status='trial'` tüm subscriptions çekilir; (2) her satır için `daysUntil(trialEndsAt)` hesaplanır; (3) `left <= 0` → atomik UPDATE `status='trial' AND trial_ends_at<=NOW()` → `status='expired'` + `invalidateFeaturesCache(companyId)` + bildirim insert; (4) `left ∈ {7,3,1}` → natural-key dedupe (entity_type='trial_warning', entity_id=subId, type=`trial_warning_${days}d`) ile bildirim — aynı sub için aynı eşik 1 kez yazılır. Bildirim mesajları TR (Başlangıç/Profesyonel/Business/Kurumsal plan adıyla zenginleştirilmiş). **Wire-up** (`api-server/src/index.ts:348-349`): `startTrialWatcher()` server boot sonrası çağrılır; `DISABLE_TRIAL_WATCHER=true` ile devre dışı bırakılabilir. **Manuel tetik** (`api-server/src/routes/auth.ts:1041-1055`): `POST /api/auth/admin/trial-watcher/run` — super_admin only — operasyonel/test amaçlı immediate run, `{ok, report}` döner. **Architect Round 1 FAIL → Round 2 PASS**: Round 1'de read-then-insert dedupe multi-instance race açıktı + cache invalidation in-process kalıyordu. Çözüm: tick-level advisory lock (PG global mutex) — yalnızca tek instance taramayı yürütür → race ortadan kalkar. Cache TTL 60s eventual consistency kabul edildi (billing-grade için Redis pub/sub Dalga 24+'de). **Empirik test**: (a) baseline 0 trial → 0 warning 0 expired; (b) seed 2 trial (1 expired + 1 with 7d) → run #1: scanned=2, warn7=1, expired=1; (c) run #2 idempotent: scanned=1 (expired sub artık 'trial' değil), warn7=0; (d) DB doğrulama: sub 1684 status='expired', notifications 2 satır (trial_expired + trial_warning_3d/7d), entity_type='trial_warning'; (e) paralel 2 concurrent endpoint çağrısı → log `trial_watcher_tick_skipped_lock_busy` ✓. Cleanup uygulandı. **Henüz eklenmedi**: e-posta/SMS dispatch (sadece in-app DB notifications) — ileri dalgada eklenecek. | TAMAM |
| 1.26 | **Dalga 19 — Usage Metering: usage_counters tablosu + e-fatura hook + /me payload (2026-04-21)** | Aylık kontör sayma altyapısı. **DB schema** (`lib/db/src/schema/usage_counters.ts` YENİ): `usage_counters` (companyId, period 'YYYY-MM' UTC, metric, count, overageCount, overageAmount numeric 12,2, lastIncrementAt) + unique index `(company,period,metric)` + companyPeriod idx. Drizzle push ✓. **Servis** (`api-server/src/services/usage.ts` YENİ): `incrementUsage(companyId, metric, by)` UPSERT (`INSERT ... ON CONFLICT DO UPDATE SET count = count + EXCLUDED.count`) + plan limit'e göre overage hesabı (count > limit ise overageCount = count-limit, overageAmount = overageCount × einvoiceOverageRate). `incrementUsageSafe()` fire-and-forget wrapper — metering hatası ana akışı (e-fatura/OCR) bloke etmez. `getUsageForCurrentPeriod()` 4 metric (einvoice/ocr/apiCalls/sms) snapshot döner. `currentPeriodUTC()` 'YYYY-MM' formatı UTC. **E-fatura hook** (`routes/einvoice.ts:604`): from-sales endpoint başarılı 201 öncesi `incrementUsageSafe(companyId, "einvoice", 1)`. **/me payload** (`auth.ts:242-251`): `usage: UsagePeriodSummary` eklendi (period + 4 metric × {count, overage}). Hata fallback null. **Frontend** (`auth-context.tsx`): `UsageInfo` + `UsageMetricSnapshot` interface'leri, AuthContext'e `usage` field. **usePlanAccess hook genişletildi**: `usage`, `usageRatio(metric)` (0..1+, sınırsız=0), `isOverUsage(metric)` (overage>0 veya count>=max). USAGE_LIMIT_MAP einvoice→maxEinvoiceMonthly, ocr→maxOcrMonthly, apiCalls→maxApiCallsMonthly. **Architect Dalga 18 PASS sonrası 4 fix de bu dalgada uygulandı**: (1) `getCompanyFeatureContext` artık `status='trial'` satırlarını da okur — backfill trial subscriptions feature seti alabilir; (2) /me query `WHERE status IN ('active','trial','grace_period')` — cancelled/suspended satır göstermez; (3) frontend `pkg_procurement` features `["customers.crm","suppliers","documents"]` ile backend'le birebir; (4) backfill loop her firma için `pg_advisory_xact_lock(0x53455450, companyId)` + recheck → onboarding ile race-safe idempotent. **Verify**: /me Talha için pkg_enterprise_v3 plan + usage payload (4 metric all 0 — henüz e-fatura kesilmedi) doğru döner. **Henüz hook'lanmadı**: OCR (Sprint 47), API call middleware, SMS — Dalga 23'te. **Architect Dalga 19 round 1 FAIL → round 2 PASS**: Round 1 overage iki ayrı statement'ta (UPSERT + sonrasında UPDATE) hesaplanıyordu → concurrent increment'larda son yazan eski snapshot ile stale overage yazabilir. Round 2'de tüm hesap **single SQL UPSERT** içine `ON CONFLICT DO UPDATE` clause'una taşındı: `count = usage_counters.count + EXCLUDED.count` + `overage_count = CASE WHEN limit<=0 THEN 0 WHEN (count+EXCLUDED.count)>limit THEN ... END` (limit/rate prepare-statement parametreleri). Linearization Postgres'in row-level lock'una bırakıldı → atomik. **Empirik race test** (5 paralel insert, limit=3, rate=0.90): final count=5, overage=2, overage_amount=1.80 = (5-3)×0.90 ✓ doğru. Mid-period plan upgrade durumunda da `limit<=0` branch ile stale overage temizlenir. | TAMAM |
| 1.25 | **Dalga 18A — 4 Paket + Kontör Şeması + 30g Trial (2026-04-21)** | Pricing strategy locked: 4 satılan paket (Başlangıç 899 / Pro 1899 / Business 2499 / Kurumsal 3799 TL aylık, yıllık = aylık×10 → 2 ay hediye) + 30 günlük Kurumsal trial. **DB schema** (`lib/db/src/schema/subscriptions.ts`): `subscription_plans`'a 6 yeni kontör/limit kolonu — `max_einvoice_monthly`, `einvoice_overage_rate` (numeric 6,2), `max_ocr_monthly`, `max_api_calls_monthly`, `max_customers`, `max_marketplace_channels` (-1 = sınırsız konvansiyonu); `company_subscriptions`'a `trial_ends_at timestamptz` (status='trial' iken bitiş tarihi). Drizzle push uygulandı, geriye uyumlu defaults ile mevcut 5 firma etkilenmedi. **Plan seed** (`api-server/src/routes/subscriptions.ts`): `PLAN_DEFS` baştan yazıldı — 4 yeni slug (`pkg_starter`, `pkg_pro`, `pkg_business_v3`, `pkg_enterprise_v3`) sektör jargonuyla tanım + her seviyede einvoice/ocr/api/marketplace kotaları (örn Kurumsal: 15K e-belge / 3K OCR / 250K API / sınırsız kanal); aşan kontör birim ücreti tier başına düşer (0.90 → 0.25 TL). Eski slug'lar (`pkg_inventory`, `pkg_trade`, `pkg_business`, `pkg_growth`, `pkg_enterprise_v2`) deprecation loop ile inactive yapılır + bağlı abonelikler `pkg_enterprise_v3`'e auto-migrate edilir (existing customer downgrade etmez). `seedSubscriptionPlansV2()` artık 6 yeni alanı da yazıyor (önceden default 0 ile sızıyordu). **Trial 21→30 gün** (`auth.ts:432`): `TRIAL_DAYS = 30`. `pkg_trial_enterprise` description "30 günlük..." + tüm kontör alanları doldu (15K e-belge / 3K OCR / sınırsız diğerleri). **Backfill** (`api-server/src/index.ts`): yeni `backfillSubscriptionsForCompanies()` — abonelik kaydı olmayan tüm şirketlere otomatik 30g trial atar (purchasing → pkg_procurement, diğerleri → pkg_trial_enterprise; idempotent, status='trial' set). Sıra: companies seed → users seed → onboarding backfill → product seed → plans seed → subscription backfill. **Verify**: API endpoint `/subscriptions/plans` 4 paket doğru kotalarla (e:100/500/3K/15K, ocr:0/100/750/3K, mp:0/3/8/-1, cust:500/5000/-1/-1). | TAMAM |
| 1.25.1 | **Dalga 18B — /me Plan Bilgisi + Frontend Plan Gating Hook (2026-04-21)** | Backend /me endpoint genişletildi (`api-server/src/routes/auth.ts`): aktif `companySubscriptions` × `subscriptionPlans` join ile `plan: { slug, name, status, isTrial, trialEndsAt, limits }` payload eklendi. `isTrial = status='trial' OR slug='pkg_trial_enterprise'`. limits objesi 10 alan (maxUsers/Products/Branches/Customers/Einvoice/OcrMonthly/ApiCalls/MarketplaceChannels/storageMb + einvoiceOverageRate string). Hata durumunda warn log + plan=null fallback (silent failure) — auth flow bozulmaz. **Frontend** (`artifacts/prosan/src/components/auth-context.tsx`): `PlanLimits` + `PlanInfo` interface'leri export, `AuthContextType.plan: PlanInfo | null` eklendi, `(user as any).plan` provider value'ya bind edildi. Yeni hook `artifacts/prosan/src/hooks/use-plan-access.ts` — `usePlanAccess()` döndürür: `isTrial`, `isTrialExpired`, `trialDaysLeft` (gün cinsinden), `hasFeature(code)` (PLAN_FEATURES static set), `meetsMinPlan(slug)` (PLAN_RANK 0-4 hiyerarşi), `isOverLimit(key, current)` ve `isApproachingLimit` (≥%80) helpers. Yeni component `artifacts/prosan/src/components/plan-gate.tsx` — `<PlanGate feature="..." minPlan="..." fallback={...}>` UI kilit sarıcı; default fallback Crown+Lock ikonlarıyla "Paketleri İncele" CTA → `/paketler`. Mevcut `<TrialGateway>` (hard-block payment) korundu; PlanGate per-component soft-gating için. **Backend gerçek koruma yine `requireFeature` middleware'inde** — frontend sadece UX. Test'ler kapsam dışı (Dalga 25'te güncellenecek). | TAMAM |
| 1.24 | **Dalga 17 — Lock Reason UI + Single Source of Truth (2026-04-21)** | Dalga 16'nın UI tarafı: kilitli menü item'ları için 3 farklı kilit nedeni (paket/rol/hesap tipi), her birine özgü ikon + renk + tooltip + CTA. **Saf helper** `artifacts/prosan/src/lib/nav-lock.mjs` (vanilla JS, framework'siz) + `nav-lock.d.mts` TS declarations + `nav-lock.ts` re-export wrapper. **getNavLockReason** öncelik sırası: **role** (en spesifik) → **accountType** → **package** → **null**. **lockUiText** her neden için TR tooltip + CTA + href: package → "Paketi Yükselt" → /pricing, role → "Yöneticiye Bildir" → /users, accountType → "Detay" → /firma-profili. **filterVisibleNavGroups** — layout.tsx'in `visibleGroups` mantığı saf helper'a çıkarıldı; **layout.tsx artık inline filter yerine bu helper'ı kullanır** (architect 2. round verdict: single source of truth gerekli). **Sidebar entegrasyonu**: mevcut `Lock` ikonu yerine reason-aware varyant — package=Lock+slate, role=ShieldOff+rose, accountType=Building2+amber. Kilitli item'lar radix `Tooltip` ile sarılı (delay=200ms, side=right); tooltip'te neden + CTA. Locked item'a tıklamak item.href yerine CTA href'ine yönlendirir. `data-lock-reason` DOM attr (e2e/QA için). **Mevcut behavior korundu** — rol/accountType uyumsuz item'lar gizlenmeye devam eder. **Tests** (11 yeni): D17-1 açık item null, D17-2 rol öncelik, D17-3 accountType uyumsuz, D17-4 purchasing whitelist, D17-5 paket yetersiz, D17-6 feature undefined skip, D17-7 lockUiText TR + href, **D17-8 filterVisibleNavGroups rol uyumsuz GİZLENİR** (regression), **D17-9 purchasing whitelist** (regression), **D17-10 boş grup atılır**, **D17-11 isItemHidden tercihi**. **Sonuç**: **514/514 PASS, fail=0, ~75s** (503 baseline + 11 yeni). nav-lock typecheck temiz (.d.mts ile). layout.tsx pre-existing super_admin comparison errors (Sprint J/K) kapsam dışı. **Architect 2-round PASS**: Round 1 FAIL (helper inline filter ile drift riski) → Round 2 PASS (layout helper'a bağlandı, single source). | TAMAM |
| 1.23.1 | **Dalga 16 Hardening — Architect FAIL → security guard (2026-04-21)** | Code review verdict: hidden plan koruması metadata-only, `/subscriptions/subscribe` (tenant admin self-serve) hala gizli plan ID'sini kabul ediyordu = privilege escalation. **Düzeltmeler**: (1) `/subscribe` route'a guard eklendi: `plan.isPublic === false` → 403 `kullanıcı seçimine kapalıdır`; `plan.requiredAccountType` mismatch → 403 `hesap tipine uygundur`. (2) `findOrCreatePlanId()` artık `isActive=true` ile filter (inaktif plan atanamaz). (3) Buyer fallback fail-closed yapıldı: `pkg_procurement` yoksa `pkg_trade`'e DÜŞMEZ (least-privilege ihlali olurdu) → 500 + error log. Business fallback sadece warn-log + pkg_growth (acceptable). (4) Yeni test **D16-4** — register/business sonrası 1..50 plan ID enumerate, gizli plan ID'lerinin 403 ile reddedildiği kanıtlanır. **Sonuç**: **503/503 PASS, fail=0, ~64s** (502 baseline + D16-4). | TAMAM |
| 1.23 | **Dalga 16 — Yetki Şeması v2 Foundation: Gizli Sistem Planları (2026-04-21)** | Kullanıcı şartnamesi: paket-bazlı erişim + rol-bazlı yetki + hesap tipi ayrımı; 50+ kullanıcı için ileri uyumlu. Bu dalga **Faz A — Foundation**: gizli plan altyapısı + register flow swap. **DB schema** (`lib/db/src/schema/subscriptions.ts`): `subscription_plans` tablosuna `is_public boolean default true notNull` + `required_account_type text nullable` kolonları. Drizzle push uygulandı (5 mevcut firma etkilenmedi, yeni kolonlar default ile geriye uyumlu). **Plan seed v3** (`api-server/src/routes/subscriptions.ts`): 5 satılan plan korundu + 2 yeni gizli plan eklendi: `pkg_trial_enterprise` (isPublic=false, sortOrder=100, tüm enterprise feature seti — 21g trial için) ve `pkg_procurement` (isPublic=false, requiredAccountType="purchasing", sortOrder=101, sadece customers.crm + suppliers + documents). `seedSubscriptionPlansV2()` artık `isPublic`/`requiredAccountType` field'larını da yazar; log mesajı public/hidden sayımını ayrı bildirir. **Public endpoint filter**: `GET /api/subscriptions/plans` artık `WHERE is_active=true AND is_public=true` — pricing/paketler/onboarding sayfalarında sadece 5 satılan plan görünür. **Yeni endpoint**: `GET /api/subscriptions/plans/all` (super_admin only) — billing yönetimi için tüm planları döner. **Register flow swap** (`auth.ts`): `register/business` artık `pkg_growth` yerine `pkg_trial_enterprise` planına atar (21g boyunca tüm feature açık); `register/buyer` artık `pkg_trade` yerine `pkg_procurement` planına atar (sadece keşif/teklif/karşılaştırma). Her ikisi de fallback'li (eski plan slug'ı bulunamazsa). **Tests**: J1 güncellendi (artık `pkg_trial_enterprise` bekler), J3 güncellendi (artık `pkg_procurement` bekler) + 3 yeni Dalga 16 testi: D16-1 (public listede gizli plan YOK + 5 satılan plan VAR + tüm dönenler isPublic=true assertion); D16-2 (super_admin endpoint gizli planları döndürür, super_admin login yoksa 401/403 fallback); D16-3 (procurement kayıt → features.includes("customers.crm","suppliers") VAR, ("sales.pos","marketplace.basic","einvoice.pro") YOK — feature izolasyonu). **Sonuç**: **502/502 PASS, fail=0, ~65s, concurrency=8** — 499 baseline + 3 yeni, sıfır regresyon. **Sıradaki dalgalar planlanmış**: D17 lock reason UI (paket/rol/hesap tipi ayrımı), D18 granüler view/manage feature split (~50 kod). Bu Faz A 50+ kullanıcıya hazırlık için yetki şeması v2'nin omurgasıdır. | TAMAM |
| 1.22 | **Dalga 15 — Web UX Bug Fix Sweep (2026-04-21)** | Kullanıcı 13 bug raporu (3 screenshot) → tek dalgada toplu fix. **Shared infra**: yeni `artifacts/prosan/src/lib/feature-labels.ts` — 31 feature kodu için TR label haritası + `labelFeature(code)` helper (snake_case sızıntısını tek noktadan çözer). **Fix listesi**: (1) `pricing.tsx`: "Aktif Plan: pkg_growth" slug leak → `plans.find(slug).name` lookup, kullanıcıya "Büyüme" gibi okunur isim; FEATURE_LABELS shared'a taşındı. (2) `settings/subscription.tsx`: `<span>{f}</span>` ham feature kodu → `labelFeature(f)`; `lmt(-1)` "∞" → "Sınırsız" (UI tutarlılığı için). (3) `paketler.tsx`: paket slug `stok`/name `Stok` → `envanter`/`Envanter` (DB pkg_inventory ile hizalı, naming inconsistency giderildi). (4) `marketplace/index.tsx`: 132 MockAcc-Sprint51 test seed sızıntısı → `isTestAccount()` helper (provider=mock+sandbox+name regex), tab sayacı sadece real, üstte amber uyarı kartı "N test/mock hesabı gizli" + "Göster"/"Gizle" toggle + "Tümünü Sil" bulk delete (sequential DELETE + toast); empty state "Henüz gerçek mağaza eklenmemiş" + Trendyol/Hepsiburada hint; sağlık kontrolü null → "Henüz çalıştırılmadı" hint. **Çıktılar**: snake_case feature kodları artık hiçbir UI ekranında görünmez (single source of truth), test hesapları üretim DB'sinde olsa bile UI'da gizli + toplu silinebilir, paket isimleri DB ile aligned, plan slug leak'i kapatıldı. Pre-existing typecheck hataları (sales/stock/uretim/verify — Sprint J/K kaynaklı) bu dalgada dokunulmadı, kapsam dışı. | TAMAM |
| 1.21 | **Dalga 13 — Mobile Theme Token Drift Prevention Test Suite (2026-04-21)** | D11/D12 architect non-blocking önerisi kalıcı koruma olarak entegre edildi. **Implementation**: `tests/integration.test.mjs` sonuna 3 yeni test (`describe "Dalga 13 — Mobile theme token drift prevention"`): (1) light primary/tint=#4F46E5, accent/success=#5EEAD4 brand assertion; (2) dark primary/tint=#818CF8 (Indigo-400), accent=#5EEAD4 assertion; (3) BRAND COMMITMENT JSDoc + Ticarium365 referans varlığı. **Implementation detay**: cross-package read (api-server suite ⇄ smsystems-mobile constants) `_drift_resolve` ile relative path; basit regex parse `scope: { ... key: "#hex" }`; helper'lar `_drift_*` prefix ile namespace'lendi (büyük integration dosyasında collision prevention). Future drift senaryosu: token değişirse test fail eder → review-time signal CI'da gate olur. **Architect PASS** — non-blocking öneri: ileride TS/AST parser sıkılaştırma + dashboard UX semantik snapshot test (D12 swap'ı kilitlemek için). **Full suite: 499/499 PASS, fail=0, ~80s, concurrency=8** — 496+3 yeni test, sıfır regresyon. | TAMAM |
| 1.20 | **Dalga 12 — Stat Card Semantic Hierarchy + Brand Drift Doc (2026-04-21)** | İki minor polish tek dalgada. **Değişiklikler**: (1) `app/(tabs)/index.tsx` stat card semantic swap: 'Kritik Stok' destructive(kırmızı) → warning(turuncu); 'Stok Yok' warning(turuncu) → destructive(kırmızı). UX hierarchy: stok=0 (durdurma) > kritik (uyarı) — daha doğru aciliyet sinyali. (2) `constants/colors.ts` üst başına BRAND COMMITMENT JSDoc: primary/tint/accent/success değerlerini, web `BrandLogo.tsx` aligned olduğunu, BrandMark'ın palette-independent olduğunu belgeler — review-time drift signal. Side fix: `light.success` `#00C9A7` → `#5EEAD4` (doc'la hizalı + dark.success ile aynı + brand teal). **Architect FAIL → fix → PASS**: ilk review JSDoc claim ile gerçek light.success token uyumsuzdu (`#00C9A7` drift) → fix → PASS. Non-blocking architect öneri: theme-token assertion test (drift detection) + stat card snapshot test. **Full suite: 496/496 PASS, fail=0, ~80s, concurrency=8**, sıfır regresyon. | TAMAM |
| 1.19 | **Dalga 11 — Mobile Dark Mode Brand Tutarlılığı (2026-04-21)** | D10 light tema mor (`#4F46E5`) brand getirmişti ama dark hâlâ mavi (`#4D9FFF`) → BrandMark sabit mor + dark dashboard mavi = dark mode'da görsel kopukluk. **Fix**: `constants/colors.ts` dark palette: `primary` ve `tint` `#4D9FFF` → `#818CF8` (Indigo-400, mor brand'in dark uyumlu varyantı, WCAG AA #0F1117 üzerine kontrast ~7.4:1), `accent` `#00C9A7` → `#5EEAD4` light ile aynı (Teal-300), `success` benzer `#5EEAD4`. Light dokunulmadı. Dark + light her ikisinde de mor primary + teal accent + mor BrandMark = tutarlı brand kimliği. **Architect PASS** — non-blocking öneri: theme-token assertion testi (drift prevention) + dark mode visual regression snapshot. **Full suite: 496/496 PASS, fail=0, ~80s, concurrency=8**, sıfır regresyon. | TAMAM |
| 1.18 | **Dalga 10 — Mobile Brand Color Graduation + DRY refactor (2026-04-21)** | Mobile palette web Ticarium365 mor brand'iyle hizalandı + D8 architect caveat'ı (TrialBanner inline query) çözüldü. **Değişiklikler**: (1) `constants/colors.ts` light: `primary` `#1A73E8` mavi → `#4F46E5` mor (web BrandLogo parite), `tint` aynı (single-source), `accent` `#00C9A7` → `#5EEAD4` teal. Single source — dashboard Today big card, StatCard primary icon, tab bar tint, RefreshControl tintColor, NotificationBell, ActivityIndicator hepsi otomatik mor. **Dark palette dokunulmadı** (`#4D9FFF` mavi kaldı — kontrast dengeli). (2) `hooks/usePaymentStatus.ts` — yeni shared hook (queryKey: 'payment-status', enabled super_admin skip, retry: false, staleTime 60s); `PaymentStatus` interface export. (3) `TrialBanner.tsx` inline useQuery silindi → hook çağrısı; **belt-and-suspenders role guard** korundu (`!user || super_admin → null`) çünkü React Query global cache logout sonrası invalidate olmuyor, stale leak prevention. **Mimari karar**: BrandMark hardcoded mor/teal — palette = UI token, BrandMark = sabit marka varlığı; token'a bağlanırsa dark mode'da mor T3 logo mavi olur ve kimlik kaybolur (web BrandLogo da hardcoded). **Architect FAIL → fix → PASS**: ilk review TrialBanner role guard kaybı + light.tint donuk kalmış → fix → PASS. Architect non-blocking öneri: `payment-status` query key'i user.id ile scope'lanabilir (cross-user stale prevention) — ileride. **Full suite: 496/496 PASS, fail=0, ~80s, concurrency=8**, sıfır regresyon. | TAMAM |
| 1.17 | **Dalga 9 — Mobile Notification Center Parite (2026-04-21)** | Web `notification-center.tsx` (174 satır popover) paterni mobile'a getirildi. **Yeni componentler**: (a) `components/NotificationBell.tsx` — `useQuery` `/notifications/count` `refetchInterval=60s`, badge >99 → `99+`, `accessibilityLabel`, `testID="notification-bell-mobile"`, `expo-router` `/notifications` push. (b) `app/notifications.tsx` — full screen FlatList (`limit=50`); `useMutation` `markRead` (PUT `/:id/read`) + `markAllRead` (PUT `/read-all`); type→icon/color map (stock_zero/low_stock/budget/einvoice — web TYPE_CONFIG parite); empty state; pull-to-refresh; unread border-left + dot + Inter_700Bold; Türkçe `timeAgo` (az önce/dk/sa/gün); `isError` state Feather alert + hata mesajı. **AuthContext genişletme**: `apiPut<T>(path, body)` eklendi (backend PUT bekliyor; mevcut `apiPost`/`apiPatch` yetersiz). **Dashboard top row**: `NotificationBell` + logout butonu yan yana, 8px gap. **Architect FAIL → fix → PASS**: ilk review'da bell `staff/viewer` için de görünüyordu ama backend GET/PUT endpoint'leri `requireAdmin` (403 + yanıltıcı boş ekran riski) — fix: `isAdmin = user?.role === "admin"` gating, `super_admin` skip dahil; `useQuery` `retry: false` + `isError` UI state. **Full suite: 496/496 PASS, fail=0, ~80s, concurrency=8** — backend kontratı dokunulmadı, sıfır regresyon. | TAMAM |
| 1.16 | **Dalga 8 — Mobil Parite MVP (2026-04-20)** | Web Ticarium365 brand kimliği + trial urgency mobile'a getirildi. **Yeni componentler**: (a) `components/BrandMark.tsx` — T3 lockup (mor #4F46E5 box + beyaz T + teal #5EEAD4 "3", web `BrandLogo.tsx` palette ile tutarlı), iOS shadow + Android elevation. (b) `components/TrialBanner.tsx` — `useQuery` + `apiGet('/payment/status')` (PROSAN `usePaymentStatus` paterni inline), `super_admin` skip, `planType==='trial'` ve `!isTrialExpired` guard, threshold renkleri (≤3 kırmızı, ≤7 turuncu, else mavi), `staleTime: 60s`, testID `trial-banner-mobile`. **Entegrasyonlar**: dashboard top row'a `BrandMark(36)` (greeting yanına) + `TrialBanner` (top row altı); login screen generic Feather "package" icon → `BrandMark(64)` + tagline "Stok & Barkod Yönetimi" → "365 gün işinizin yanında" (web landing parity). **Bonus backend test fix** (Sprint E flake): `/buyer/sellers` discovery testi accumulated test seed (112 eligible seller) default `limit=50`'yi aşıyordu, NIHAT (id=2 alfabetik N) sonuçtan düşüyordu — test `?q=NIHAT` ile daraltıldı (route mantığı dokunulmadı, ilike Turkish-aware). **Architect verdict: PASS** — payment-status null/error silent (banner gizli, doğru), companyId leak yok, severe issue yok. Caveat: TrialBanner inline query (shared hook abstract edilmedi) — non-blocking. **Full suite: 496/496 PASS, fail=0, ~80s, concurrency=8** — Dalga 7+8 birlikte sıfır regresyon. Pre-existing `<T>` generic .tsx parsing TS hatası (commit a1e35b6 AuthContext) Babel runtime'da tolere ediliyor. | TAMAM |
| 1.15 | **Dalga 7 — Sidebar Mockup → PROSAN Selektif Graduation + Critical Bug Fix (2026-04-20)** | Canvas'taki Ticarium365 Sidebar mockup'ından (`artifacts/mockup-sandbox/.../ticarium365/Sidebar.tsx`, 132 satır basit demo) görsel detayları PROSAN'ın production-grade kompleks layout'una (feature gates + accountType + collapse + mobile sheet) **minimal müdahale ile** entegre edildi — fonksiyonel mantık dokunulmadı. **Değişiklikler (`layout.tsx`)**: (a) Sub-item active state: `rounded-md + border-l-2 border-blue-500` → `rounded-xl + shadow-sm shadow-blue-500/10` (mockup pattern, daha modern). (b) Desktop footer user widget: avatar `rounded-full` → `rounded-lg + shadow-sm`, role label kaldırıldı, yerine `Building2 + companyName` tenant context (mockup'taki "PROSAN ENDÜSTRİ" feel); container `hover:bg-slate-50 + rounded-xl` transition. Yeni testid'ler: `sidebar-user-name`, `sidebar-tenant-name`. (c) Mobile sheet footer'a aynı tutarlılık (avatar rounded-lg + tenant satırı). **Kritik bug fix**: `entegrasyonlar.tsx` Dalga 5'ten kalan `import { useAuth } from "@/hooks/use-auth"` → `"@/components/auth-context"` (yanlış path mevcut değil; vite import-analysis 500 hatası veriyordu, frontend görsel test'te yakalandı). **Architect verdict: PASS** — selektif graduation doğru uygulandı, layout overflow `min-w-0 + truncate` ile mitigate, XSS yok (React escape), testid duplicate yok, dark mode etkisi minimal, severe issue yok. **Full suite: 496/496 PASS, fail=0, ~67s, concurrency=8** — sıfır regresyon. | TAMAM |
| 1.14 | **Dalga 6 — B2B Akış UX Cilası (2026-04-20)** | Quote→Order otomasyonu (b2b.ts decision endpoint accept'te otomatik `b2b_orders` insert'leyip `{orderId, orderCode}` döndürüyor — Sprint F'ten zaten mevcut) backend'i değişmedi, frontend UX iki noktada cilalandı. **Değişiklikler**: (a) `quote-detail.tsx` `decide(accepted)` handler — backend yanıtından `Number(data?.orderId)` çekilip `Number.isInteger(oid) && oid > 0` strict guard ile valide ediliyor (architect önerisi: open-redirect/NaN/string-injection koruması), valid ise toast'a `orderCode` bilgisi yazıp `navigate('/b2b/orders/'+oid)` ile direkt yeni siparişe yönlendiriliyor; invalid/missing orderId veya rejected'da fallback fetchData refresh. (b) `order-detail.tsx` İşlemler kartına `isSeller && (status==='delivered' \|\| status==='completed')` koşulunda "Fatura Kes" wouter Link CTA (testid `cta-b2b-order-invoice`, /einvoice'a gidiyor); buyer'a görünmez (UX gate, backend role check ayrı katman). **Architect verdict: PASS** — open-redirect riski yok (path hard-prefix), seller-only doğru, status guard doğru, severe issue yok, sadece orderId strict numeric validation hardening önerildi → uygulandı. **Full suite: 496/496 PASS, fail=0, ~73s, concurrency=8** — sıfır regresyon. | TAMAM |
| 1.13 | **Dalga 5 — Entegrasyon Derinliği MVP (2026-04-20)** | `/entegrasyonlar` showcase sayfası gerçek backend'e bağlandı. **3 değişiklik**: (a) **Bug fix App.tsx route shadowing** — `/settings/integrations` yanlışlıkla EntegrasyonlarPage'e mount edilmiş ve gerçek `IntegrationsPage`'i (admin-only yönetim ekranı) wouter ilk-match kuralı ile gölgeliyordu; ilk mount kaldırıldı. (b) **settings/integrations.tsx ?tab= deep-link desteği** — useState init query string'den `accounting/ecommerce/api-keys/webhooks` allowlist ile okunuyor (XSS-safe URLSearchParams). (c) **entegrasyonlar.tsx komple yeniden yazıldı** — useQuery ile `GET /api/ext-integrations/{accounting,ecommerce}` çağırılıyor (admin için), connected status DB'den hesaplanıyor (provider/platform id mapping: parasut→parasut, logo-tiger→logo, mikro→mikro, luca→luca, netsis→netsis; trendyol/hepsiburada/n11/pazarama/shopify/woocommerce ecommerce mapping'i 1:1); "Bağla"/"Yönet" butonları wouter `<Link href="/settings/integrations?tab=...">` ile deep-link yapıyor; admin olmayanlar için "Yetkisiz" disabled state; demo notu kaldırıldı, yerine "Yönetim Ekranını Aç" CTA. **Architect verdict: PASS** — auth+route güvenliği doğru (backend `requireRole(["admin"])` ile FE/BE iki katmanlı), XSS yok, mapping doğru, edge-case'ler (admin değilken accQ disabled, accQ.data?.integrations ?? []) kapatıldı; severe issue yok. **Full suite: 496/496 PASS, fail=0, ~73s, concurrency=8** — sıfır regresyon. | TAMAM |
| 1.12 | **Sprint J — Test Tenant-Switch Fix (2026-04-20)** | Sprint J (1.7) integration testlerinde regresyon yakalandı: Test helper `CookieJar` her istekte `X-Tenant: prosan` header'ı gönderiyor → register/business yeni şirket yaratsa bile sonraki `/auth/me` ve `/subscriptions/current` çağrıları PROSAN tenant'ına düşüp prosan'ın aktif planını döndürüyor. **Fix**: J1/J3/J4/J5 testlerinde register sonrası `jar.tenant = r.json.subdomain` ataması eklendi → sonraki çağrılar yeni şirket subdomain'ine yönlendiriliyor (5 satır). Implementation/schema değişmedi, gerçek bir backend hatası yoktu — test izolasyonu tamamlandı. **Architect verdict: PASS** (txn correctness/atomicity/security controls yeniden doğrulandı, hashed OTP + bcrypt + 5x lockout + 60s cooldown güvenlik kontrolleri yerinde, severe issue yok). **Full suite: 496/496 PASS, fail=0, ~83s, concurrency=12** — sıfır regresyon. | TAMAM |
| 1.11 | **Sprint N — Toptan / Perakende Satış Sınıflandırması (2026-04-20)** | Satış akışına `saleType` ('retail' \| 'wholesale') eklendi. **DB**: `lib/db/src/schema/sales.ts` `saleType: text("sale_type").default("retail").notNull()` + `sales_company_saletype_idx` (companyId, saleType); `db:push --force` ✓. Mevcut 738 kayıt akıllı backfill: müşterili + non-POS → wholesale (464), kalan → retail (274). **Backend**: `routes/sales.ts` GET `?saleType=retail|wholesale` filter; POST body'sine `saleType` + `channelKey` (kullanıcı override yoksa akıllı default — channel=pos veya müşteri yok → retail, cari müşterili → wholesale). `routes/reports.ts` `/reports/sales` response'una `saleTypeBreakdown` ({retail/wholesale × count/quantity/revenue/profit}); CSV export'a "Satış Tipi" kolonu (Toptan/Perakende). `routes/dashboard.ts` `/stats` response'una `todayWholesaleRevenue/Count` + `todayRetailRevenue/Count`. **OpenAPI**: `lib/api-spec/openapi.yaml` listSales `saleType` query param + Sale schema `saleType/channelKey` + CreateSaleBody `saleType/channelKey/paymentMethod/customerId`; orval codegen ✓. **Frontend (PROSAN)**: `pages/sales/pos.tsx` "Satış Tipi" segmented control (Perakende/Toptan, default Perakende) + checkout body'ye `saleType, channelKey:"pos"`; `pages/sales/history.tsx` `saleType` query param entegrasyonu; `pages/dashboard.tsx` 2 yeni KPI kartı ("Bugün — Toptan" indigo + "Bugün — Perakende" amber, `kpi-wholesale-revenue` / `kpi-retail-revenue` testid). **Test**: 3 yeni `Sprint M+ — Sales Type` describe (M+1 explicit wholesale 201, M+2 POS default retail, M+3 GET filter sadece wholesale döner) **3/3 PASS**. **Full suite: 496/496 PASS, fail=0, ~71s — sıfır regresyon**. | TAMAM |
| 1.10 | **Sprint M — SMSYSTEMS → Ticarium365 Marka Yenileme (2026-04-20)** | Kullanıcı talebi: "SMSYSTEMS artık Ticarium365 oldu. PROSAN sadece test dükkan adı, ona dokunma." Kullanıcıya görünen tüm SMSYSTEMS string'leri Ticarium365 ile değiştirildi: (1) **Backend**: `routes/public-api.ts` `name: "Ticarium365 Public API"`; `routes/finance-documents.ts` öneri inbound adres `belgeler+co${id}@ticarium365.com`; `routes/imports.ts` ve `tests/integration.test.mjs` header comment'leri. (2) **DB schema**: `lib/db/src/schema/finance_documents.ts` comment. (3) **Mobile artifact**: `.replit-artifact/artifact.toml` title "SMSYSTEMS Mobil" → "Ticarium365 Mobil" (verifyAndReplaceArtifactToml ile güvenli değişim). (4) **Prosan public routes**: redundant `/neden-smsystems` route + auth-context publicPaths whitelist'inden temizlendi (`/neden-ticarium365` zaten KarsilastirPage render ediyor, aynı sayfaya 2 URL gerekmez). (5) **Pitch docs script** `generate-pitch-docs.mjs` Mobil App satırı. **DOKUNULMADI**: artifact dizin adları (`smsystems-mobile`), workspace package adları (`@workspace/smsystems-mobile`), Expo `slug`/`scheme`/`previewPath` (build kimliği — değiştirilirse workflow + bundle ID kırılır), AuthContext storage key'leri (mevcut kullanıcı oturumları invalid olmaması için), `attached_assets/` ham yapıştırılmış geçmiş raporlar, replit.md/docs eski sprint kayıtları (audit log). **Test**: 493/493 PASS, fail=0, ~67s — sıfır regresyon. | TAMAM |
| 1.9 | **Sprint L — Mobile Login Polish + Public Routes Bug Fix (2026-04-20)** | Mobil login UI cilalama: (1) PublicNav zaten logo gösterdiği için login.tsx'teki duplicate mobile-only logo bloğu kaldırıldı, (2) outer padding `p-6 md:p-8` → `p-4 md:p-8`, (3) form kart padding `p-7 md:p-8` → `p-5 sm:p-7 md:p-8`, (4) segmented control etiketleri mobile (`<sm`) için "İşletme" / "Satınalmacı" kısa form, sm+ için "İşletme Girişi" / "Satınalmacı Girişi" full form. **KRİTİK BUG FIX**: `pages/hakkimizda.tsx` `NAVY` constant import edilmemişti → mobilde `ReferenceError: NAVY is not defined` ile sayfa crash; `const NAVY = "#0F172A"` eklendi. **DAHA KRİTİK BUG FIX**: `components/auth-context.tsx` `publicPaths` listesinde `/kayit`, `/verify`, `/kullanim-kosullari` eksikti → 401 (oturum yok) durumunda otomatik `setLocation("/login")` tetiklenip kayıt sayfası asla render edilemiyordu. Liste tamamlandı. **Test**: 493/493 PASS, fail=0, 68s — regresyon yok. | TAMAM |
| 1.8 | **Sprint K — Entegrasyon Merkezi + ComingSoon + UI Cleanup (2026-04-20)** | **PHASE: Membership + Portal + UI Cleanup + Integration Ready / Dalga 2-4**. Wave 2 (Landing/PageHeader): Mevcut sayfalar zaten PageHeader ile aynı CSS sınıflarını (`t365-gradient-text t365-heading-accent` + `font-display`) kullandığı tespit edildi → görsel tutarlılık zaten sağlanmış, gereksiz migration atlandı. Public landing sayfaları (hakkimizda, amacimiz, paketler, iletisim, karsilastir, neden-ticarium365) zaten Ticarium365 markası ile tutarlı, PROSAN brand sızıntısı yok. Wave 3 (Yetki + ComingSoon): Yeni `components/coming-soon.tsx` reusable component (PageHeader + dashed-card + roadmap features list + ETA badge + iletişim CTA), ileride henüz açılmayan modüller için drop-in fallback. nav-config'teki tüm linkler için App.tsx route varlığı doğrulandı (comm karşılaştırma → 0 eksik). Wave 4 (Entegrasyon Merkezi): Yeni `pages/entegrasyonlar.tsx` (25 entegrasyon — pazaryerleri Trendyol/Hepsiburada/n11/Çiçeksepeti/Amazon, ödeme iyzico/Param/Garanti/Ziraat, e-fatura Uyumsoft/Logo/QNB, kargo Yurtiçi/Aras/MNG/PTT, mesajlaşma NetGSM/İletiMerkezi/SendGrid/WhatsApp, muhasebe Logo Tiger/Mikro/Paraşüt, analitik GA4/Meta Pixel) + 7-kategori filter chips + arama + 4-stat özet (toplam/bağlı/hazır/yakında) + status badge'leri (connected/available/coming_soon) + Bağla/Yönet/Bilgi al CTA + demo modu uyarı banner'ı. Route: `/entegrasyonlar` (tüm roller) + `/settings/integrations` (admin) ikisi de aynı sayfayı render eder. Sidebar Yönetim grubunda eski "/settings/integrations" link'i "Entegrasyon Merkezi" olarak yeniden etiketlendi ve `/entegrasyonlar`'a yönlendirildi. **Test**: Mevcut 493 test takım hızı 68s, **493/493 PASS, fail=0** (yeni Entegrasyon Merkezi backend mock-only, regresyon yok). | TAMAM |
| 1.7 | **Sprint J — Membership / Public Registration + Verification (2026-04-20)** | **PHASE: Membership + Portal + UI Cleanup + Integration Ready / Dalga 1**. Public self-service kayıt akışı PROSAN/Ticarium365 yönetim panelinden çıktı, açık web'e taşındı. **DB**: `verification_tokens` tablo (channel sms/email, target, codeHash, attempts, consumed, expiresAt, ipAddress, userAgent + idx target_channel/userId/expiresAt) + `companies.{trialStartedAt, verificationMethod, district, emailVerifiedAt, phoneVerifiedAt}` Sprint J alanları (önceden hazırdı), `pnpm db:push --force` ✓. **Backend (`routes/auth.ts`)**: 4 yeni public endpoint — `POST /auth/register/business` (21g trial + pkg_growth/yearly seed + Turkish→ASCII slugify subdomain + collision suffix `-2,-3,...` + atomik txn (companies+users+companySubscriptions+verificationToken) + 23505→409 mapping + per-IP rate-limit (prod 5/10min, dev/test bypass) + KVKK consent zorunlu + audit REGISTER_BUSINESS_COMPLETED + auto-session); `POST /auth/register/buyer` (accountType=purchasing + pkg_trade/monthly + trial yok); `POST /auth/verify/send` (oturum gerekli + 60sn cooldown + kanal seçimi + eski kodları consume); `POST /auth/verify/check` (bcrypt compare + 5x→429 + companies.{email,phone}_verified_at set). 6 haneli code 10 dk TTL bcrypt hash; SMS/email factory fallback (DEV mode'da kod logger.warn ile düşer). **Frontend**: `pages/register.tsx` (mode select kartları İşletme/Satınalmacı + form tek dosyada + verificationMethod radio + KVKK checkbox + /dashboard veya /satinalma-merkezi redirect); `pages/verify.tsx` (6-digit input + 60sn resend cooldown + done state); `pages/login.tsx` (segmented control "İşletme Girişi" / "Satınalmacı Girişi" + "Demo talep edin" → "Hemen kayıt olun" → /kayit); `components/public-nav.tsx` (sağ üst "Kayıt Ol" CTA + mobile menu); `App.tsx` route wiring (/kayit, /kayit/isletme, /kayit/satinalmaci, /verify). **Test**: 5 yeni `Sprint J — Membership + Verification` describe (J1 register/business 201+pkg_growth+trial≈+21d, J2 dup email 409, J3 buyer 201+accountType=purchasing, J4 wrong code 400, J5 5x wrong→429) **5/5 PASS**. **Full suite**: 481 baseline + 5 yeni = 486 PASS, fail=0, 4-chunk paralel doğrulama. Risk alanları (rate-limit + trial atomicity + Sprint 11 STRICT after() çakışması) izole edildi: rate-limit env-gated, txn atomik (subscription içinde), her test unique stamp ile yeni şirket (mevcut prosan/nihat etkilenmiyor) | TAMAM |
| 1.6 | **Sprint I Phase 1 — Satınalma Merkezi şartname revizyonu** | Schema: `companies.city` (text) + `companies.isVerified` (bool) eklendi (drizzle push); `/b2b/catalog/marketplace` response'a `companyCity / companyVerified / productBrand / productStock` alanları + filtreler `city, brand, minOrderQty≤, fastOnly (leadDays≤3), certifiedOnly`; yeni endpoint'ler `/marketplace/cities` + `/marketplace/brands` (sadece aktif vitrin firmaları); frontend tablo kolon düzeni şartname uyumlu — **Ürün · Firma · Kategori · Şehir · Fiyat · Stok · Teslim Süresi · İşlem**; ekstra filtreler (Şehir/Marka select, Min sip. ≤ input, Hızlı/Sertifikalı/Favori checkbox); satır aksiyonları (Teklif İste · Firma Profili `/magaza/:subdomain` yeni tab · Favori · İletişim · Karşılaştır); Sertifikalı rozeti (ShieldCheck) firma adı yanında; Hızlı teslim ikonu (Zap) leadDays≤3'te; xlsx export 11 kolon (Marka · Şehir · Sertifikalı · Stok dahil); Sprint A regression 6/6 PASS (Phase 1 değişiklikleri non-breaking) | TAMAM |
| 2 | **Sprint B — Notification Hub Entegrasyonu** | `services/notifications/dispatch.ts` tek noktadan in-app yazım + günlük dedup (pg `pg_advisory_xact_lock` ile concurrent serileştirme); `dispatchBudgetAlerts` (severity → `budget_alert_critical/warning/info`, `entityType="budget"`, **`hashPeriod(period, type, categoryId)` → period+type+categoryId boyutuna duyarlı dedup** — aynı kategori farklı dönem aynı gün içinde AYRI kayıt); `dispatchEinvoiceEvent` (`einvoice_sent/failed/cancelled`, `entityType="einvoice_outbox"`, `bypassDedup=true` → status değişimleri her seferinde görünür); wiring: `routes/budgets.ts` recompute + `routes/einvoice.ts` send/cancel branch'ları; frontend `pages/notifications/index.tsx` ICON_MAP'e 6 yeni tip + entity tipi tabanlı deep-link (`/butce`, `/einvoice?outbox=:id`); `pages/einvoice/index.tsx` `?outbox=:id` query parse → `outbox-row-${id}` scroll + 3.5s primary-ring highlight pulse; regression `tests/integration.test.mjs` describe "Sprint B — Bütçe alarmları + e-fatura olayları → bildirim merkezi" **5/5 PASS** (dispatch idempotency, budget_alert_* listede görünür, einvoice send sonrası notification, entityId değişince yeni kayıt, **fixture-controlled period dimension dedup → disjoint entityId**); architect 2. round PASS | TAMAM |
| 3 | **Sprint C — Marketplace Worker UI** | `routes/marketplace.ts` `parseJobMeta(row)` türetilmiş alanlar: `errorCategory` (`rate-limit\|permanent\|transient`, lastError prefix `[kalıcı/rate-limit/geçici]`'den parse), `errorMessage` (cleaned), `nextRetryAt` (ISO, retry penceresi açıkken), `retryAvailable` (boolean: scheduledAt gelecekte + attempt<max + non-permanent + non-completed); `GET /marketplace/jobs` her satıra spread eder; frontend `pages/marketplace/index.tsx` `JobsTab` tek paylaşımlı 1s tick (per-row interval yerine), `JobRow` sub-component: status badge (completed=default, failed=destructive, queued/running=secondary), kategori rozetleri (Kalıcı=AlertOctagon destructive, Hız Limiti=Hourglass amber-500, Geçici=Clock secondary), `attemptCount/max` exhausted durumda destructive bold, retry timer canlı geri sayım (`Xd YYs sonra`, font-mono tabular-nums amber-600), permanent için "Tekrar yok", limit dolduysa "Limit doldu"; data-testid: `job-cat-${id}`, `job-retry-${id}`, `job-row-${id}`; regression `tests/integration.test.mjs` "Sprint C — GET /marketplace/jobs response'unda errorCategory + nextRetryAt + retryAvailable türetilmiş alanları yer alır" **1/1 PASS** | TAMAM |
| 4 | Sprint D (eski 71) — Buyer Portal Foundation | Schema (`accountType`, `buyer_*`), buyer-portal artifact iskeleti, auth | TAMAM (Sprint H ile birleşti) |
| 5 | Sprint E (eski 72) — Buyer Discovery + RFQ | Firma/ürün listesi + multi-seller RFQ submit + satıcı RFQ Inbox + lead düşürme | TAMAM |
| 6 | Sprint F (eski 73) — Quote Response & Comparison | Satıcı yanıt formu + buyer karşılaştırma ekranı + accept/reject + audit | TAMAM |
| 7 | Sprint G — POS → E-Fatura "Satıştan Otomatik" UI | Backend `POST /einvoice/from-sales` (Sprint 62) — `saleIds: number[]` validasyonu (boş→400, geçersiz→400, var olmayan→404, müşterisiz satış→400, viewer rolü→403), aynı müşteriye ait satışları birleştirir, sender'ı `einvoice_settings.config`'ten okur (`senderVkn` zorunlu), receiver'ı müşteri tablosundan kurar, satış satırlarını UBL line'larına map'ler (default %20 KDV), `einvoice_outbox` row insert eder + provider üzerinden `lastResponse.xml` üretir, `outbox.saleId` ile origin trace; frontend `pages/sales/history.tsx` her satışa müşterili ise `quick-invoice-${id}` butonu (FileText ikonu blue-600), `openQuickInvoice(saleId)` Dialog açar (max-w-3xl), `previewLoading` Loader2 spinner, `previewOutbox` döndüğünde Alıcı/Toplam/Para birimi grid + slate-950 dark XML viewer (`einvoice-xml-preview` testid, font-mono whitespace-pre-wrap), durum mesajı, "E-Fatura Listesine Git" navigation + "Gönder" butonu (draft/failed/queued statülerinde aktif); **Architect 4. round hardening (II) — RESERVE-FIRST + provider hata cleanup + deterministik polling**: (a) `uniqueIds.length !== sales.length` → **404 + missing[]** (kısmi liste sessizce devam etmiyor); (b) **uygulama-katmanı idempotency**: tek saleId için iptal/red dışı outbox varsa sent/accepted/sending → **409 + existingOutboxId**, draft/queued/failed → **200 + reused=true**; (c) **DB-katmanı race-safe garanti**: `einvoice_outbox_active_per_sale_idx` partial unique index `(companyId, saleId) WHERE status NOT IN ('cancelled','rejected')` — `'reserving'` da unique kapsamında; (d) **RESERVE-FIRST pattern**: önce `status='reserving'` placeholder INSERT atılır → DB index yarışı arbitrate eder → SADECE kazanan `provider.createInvoice` çağırır (mükerrer harici fatura imkânsız) → row provider sonucuyla update edilir; kaybedenler 23505 alır + 25ms×40 polling ile kazananı bekler — settled (status !== 'reserving') görürse **200 + reused=true + raceWon=false**, polling timeout sonu hâlâ 'reserving' ise yanıltıcı başarı vermez **503 + reserve_pending + Retry-After: 1**; (e) **provider hata cleanup**: winner provider.createInvoice throw ederse placeholder 'failed'e çekilir (statusMessage + lastResponse.error yazılır) → satır asla 'reserving'de kilitli kalmaz → idempotency-katmanı 'failed' satırı 200/reused olarak döndürdüğü için sonraki istekler reuse edip kullanıcıya retry imkânı verir; (f) test sertleştirildi (`__test_mock_counter` + `__test_mock_fail_next` endpoint'leri, NODE_ENV !== production'da açık): sender VKN PUT setup + 201 zorunlu + idempotent reuse aynı id + sent → 409/existingOutboxId + 5 paralel POST → `Set(ids).size === 1` + tam 1 created/4 reused + **KRİTİK: `provider.createInvoice` tam 1 kez çağrıldı** invariant; regression "POS → from-sales köprüsü — validation kontratı" suite altında **11/11 PASS** (validation 5 + happy 1 + sent-409 1 + concurrency-with-counter 1 + partial 1 + provider-fail-cleanup 1 + reserve-pending-503 1) + (g) **pre-check 'reserving' guard**: idempotency pre-check'i de aynı bounded polling + 503 davranışını uygular → late-loser hiçbir branch'te 'reserving' satırını 'reused' olarak görmez; **Architect 4. round (III) PASS** (her iki blocker — provider-error cleanup + reserving-asla-reused — hem 23505 collision hem pre-check branch'inde kapatıldı) | TAMAM |
